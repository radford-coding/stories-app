<%- include('./partials/_htmlhead') %>
  <title>home</title>
</head>
<body class="index-body">
  <%- include('./partials/_navbar') %>

  <div class="index-container text-center m-4">
    <% if (user) { %>
      <h1>come on in, <%= user.username %>, grab a story and relax</h1>
    <% } else { %>
      <h1>welcome to the stories app, guest.</h1>
      <p>
        <a href="/auth/register">register</a> or
        <a href="/auth/login">log in</a>.
      </p>
    <% } %>
  </div>
</body>
</html>